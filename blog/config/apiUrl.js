const baseClientUrl = 'http://localhost:7001/client/'

const clientPaths = {
    getArticleList: `${baseClientUrl}get_list`,
    getDetail: `${baseClientUrl}get_detail/`,
    getTypes: `${baseClientUrl}get_types`
}

export default clientPaths

